import { createTheme } from "@mui/material/styles";
export const theme = createTheme({
  palette: {
    button: {
      background: "#15BCCB",
      border: "#2B8791",
      color: "#fff",
    },
    gray: {
      main: "#8895A6",
    },
    background: "#EFF5F5",
    white: {
      main: "#fff",
    },
    chips: {
      lead: {
        background: "#AFC6F0",
        color: "#2459BB",
      },
      client: {
        background: "#B4E9D2",
        color: "#2D6E57",
      },
      prospect: {
        background: "#EBBDA0",
        color: "#AB1F07",
      },
    },
  },
});
